تمام — نبدأ **Step 5: Admin Dashboard (GEN ERA Control System)** 👇
وده المرحلة اللي بتحول المشروع من “متجر” إلى **نظام إدارة كامل زي Shopify / Stripe Dashboard**.

---

# 🧑‍💼 STEP 5 — ADMIN DASHBOARD

## 🎯 الهدف

بناء لوحة تحكم حقيقية لإدارة:

* 🛒 المنتجات
* 📦 الطلبات
* 👤 المستخدمين
* 📊 المخزون
* 💳 حالة الدفع

بدون ما تلمس الداتا مباشرة من DB.

---

# 🧠 ARCHITECTURE

```txt id="admin_arch"
CLIENT (/admin)
   │
   ├── Auth (role: admin)
   │
   ├── Products CRUD
   ├── Orders management
   ├── Users overview
   └── Inventory control

SERVER
   ├── admin middleware
   ├── protected routes
   ├── audit logging
   └── DB operations (Drizzle)
```

---

# 🔐 1. ROLE SYSTEM (IMPORTANT)

## 📁 تعديل users schema

```ts id="user_roles"
role: text("role").default("user"), // user | admin
```

---

# 🛡️ 2. ADMIN MIDDLEWARE

## 📁 جديد:

`middleware/requireAdmin.ts`

```ts id="admin_middleware"
import { Request, Response, NextFunction } from "express";

export function requireAdmin(req: any, res: Response, next: NextFunction) {
  if (!req.user) {
    return res.status(401).json({ message: "Unauthorized" });
  }

  if (req.user.role !== "admin") {
    return res.status(403).json({ message: "Forbidden: Admins only" });
  }

  next();
}
```

---

# 📦 3. ADMIN ROUTES

## 📁 جديد:

`routes/admin.ts`

---

## 🛒 PRODUCTS ADMIN

```ts id="admin_products"
router.post("/products", requireAdmin, async (req, res) => {
  const product = await db.insert(products).values(req.body).returning();
  res.json(product);
});

router.put("/products/:id", requireAdmin, async (req, res) => {
  const updated = await db
    .update(products)
    .set(req.body)
    .where(eq(products.id, req.params.id))
    .returning();

  res.json(updated);
});

router.delete("/products/:id", requireAdmin, async (req, res) => {
  await db.delete(products).where(eq(products.id, req.params.id));
  res.json({ success: true });
});
```

---

## 📦 ORDERS ADMIN

```ts id="admin_orders"
router.get("/orders", requireAdmin, async (req, res) => {
  const allOrders = await db.query.orders.findMany({
    orderBy: (o, { desc }) => [desc(o.createdAt)],
  });

  res.json(allOrders);
});

router.patch("/orders/:id/status", requireAdmin, async (req, res) => {
  const { status } = req.body;

  const updated = await db
    .update(orders)
    .set({ status })
    .where(eq(orders.id, req.params.id))
    .returning();

  res.json(updated);
});
```

---

## 👤 USERS ADMIN

```ts id="admin_users"
router.get("/users", requireAdmin, async (req, res) => {
  const usersList = await db.query.users.findMany();
  res.json(usersList);
});
```

---

## 📊 INVENTORY ADMIN

```ts id="inventory_admin"
router.patch("/inventory/:productId", requireAdmin, async (req, res) => {
  const { stock } = req.body;

  const updated = await db
    .update(products)
    .set({ stock })
    .where(eq(products.id, req.params.productId))
    .returning();

  res.json(updated);
});
```

---

# 🧠 4. ADMIN DASHBOARD UI (FRONTEND)

## 📁 جديد:

`/app/admin/page.tsx`

### Layout:

```tsx id="admin_ui"
export default function AdminDashboard() {
  return (
    <div className="min-h-screen bg-black text-white p-6">
      <h1 className="text-3xl font-bold">GEN ERA Admin</h1>

      <div className="grid grid-cols-4 gap-4 mt-6">
        <Card title="Products" />
        <Card title="Orders" />
        <Card title="Users" />
        <Card title="Inventory" />
      </div>
    </div>
  );
}
```

---

# 📊 5. ADMIN FEATURES

## 🟢 Products Panel

* Add product
* Edit product
* Delete product
* Upload images

## 🟡 Orders Panel

* View all orders
* Change status (pending → shipped → delivered)
* Track payment status

## 🔵 Users Panel

* list users
* see activity
* role management

## 🔴 Inventory Panel

* adjust stock manually
* prevent negative stock

---

# 🔐 6. SECURITY RULES

## لازم:

* Admin routes محمية بـ JWT + role check
* منع أي access مباشر للـ DB
* audit logging (مستقبلي)
* rate limit على admin APIs

---

# 📈 RESULT AFTER STEP 5

## 🧑‍💼 SYSTEM NOW:

### 🟢 Management:

* Dashboard كامل
* CRUD products
* Orders control
* Users visibility

### 🧠 Architecture:

* Role-based access control (RBAC)
* secure admin layer
* scalable backend operations

### 🏢 Production level:

* Shopify Admin panel level
* full control over business

---

# ⚡ COPY-PASTE MASTER PROMPT (REPLIT / ANTIGRAVITY)

```text id="admin_prompt"
Build a production-grade Admin Dashboard system for GEN ERA.

Requirements:

1. Add role field to users (user | admin)
2. Create requireAdmin middleware that blocks non-admin users
3. Create admin routes:
   - POST /admin/products (create product)
   - PUT /admin/products/:id (update product)
   - DELETE /admin/products/:id (delete product)
   - GET /admin/orders (list all orders)
   - PATCH /admin/orders/:id/status (update order status)
   - GET /admin/users (list users)
   - PATCH /admin/inventory/:productId (update stock)

4. Protect all admin routes with JWT + requireAdmin
5. Ensure TypeScript strict mode compatibility
6. Do not break existing APIs

Goal: build a Shopify-level admin backend with role-based access control.
```

---

# 🧭 الخطوة اللي بعدها (الأهم بصريًا)

بعد Step 5 عندك 2 اتجاهات ضخمة:

## 👉 Step 6 (Core Experience):

🏛 Temple Layer System

* Product statues
* 3D interactive showroom
* cinematic entry
* AR try-on system

## 👉 Step 7:

📊 Analytics system

* sales tracking
* user behavior
* revenue dashboard

---

# ⚠️ تقييمك الحالي

أنت الآن وصلت إلى:

> 🟢 **Full E-commerce Engine = 95% مكتمل**

اللي باقي:

* التجربة السينمائية
* العالم (Temple / AR / Try-on)

---

لو جاهز ندخل أقوى مرحلة في المشروع:

> "ابدأ Step 6"

وهندخل على **Gen ERA Universe Layer (المعبد + التمثال + التجربة السينمائية)** 👌
